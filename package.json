{
  "name": "proyecto1-billboard-hot-100",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/prepare-data.mjs",
    "predev": "npm run prepare:data",
    "dev": "nuxt dev",
    "prebuild": "npm run prepare:data",
    "build": "nuxt build",
    "pregenerate": "npm run prepare:data",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "validate:project": "npm run prepare:data && node scripts/validate-data.mjs"
  },
  "dependencies": {
    "nuxt": "^4.5.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  }
}
